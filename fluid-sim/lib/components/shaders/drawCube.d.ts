import { Renderer } from '../../renderer';
import { RenderState } from '../../types';
export declare const createDrawCube: ({ regl }: Renderer, n: number, colorProfile: string) => ({ world, config }: RenderState) => void;
